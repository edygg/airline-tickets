<%= form_for(@airplane, html: { class: 'form-horizontal' }) do |f| %>
  <% if @airplane.errors.any? %>
    <div class="alert alert-warning">
      <p><%= pluralize(@airplane.errors.count, "error") %> prohibited this airplane from being saved:</p>

      <ul>
      <% @airplane.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :airplane_model, class: 'control-label col-sm-4' %>
    <div class="col-sm-8">
      <%= f.text_field :airplane_model, class: 'form-control' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
